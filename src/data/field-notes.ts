export type FieldNote = {
  slug: string;
  title: string;
  sourceTitle: string;
  sourceUrl: string;
  takeaway: string;
  date: string;
};

// Empty until real articles/incidents are curated. Add entries here as they're written.
export const fieldNotes: FieldNote[] = [];
